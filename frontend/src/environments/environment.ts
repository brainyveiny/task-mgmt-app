/**
 * @file environment.ts
 * @description Development environment configuration
 * Replace apiUrl with your production URL when deploying.
 */
export const environment = {
    production: false,
    apiUrl: 'http://localhost:8000'
};
